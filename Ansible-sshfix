pipeline {
    agent any
    environment {
     ANSIBLE_PRIVATE_KEY=credentials('automation-key') 
  }

    
    stages {
        stage('Cleanup Junk data') {
            steps {
                script {
                    
                        sh """
                        
                        ansible-playbook -i inventory/hosts  playbooks/sshfix.yaml --private-key=$ANSIBLE_PRIVATE_KEY
                        """
                    
                }
            }
        }
    }
}
