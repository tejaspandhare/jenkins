pipeline {
    agent any
    stages {
        stage('Check Users') {
         steps {
          sh "ansible-playbook -i inventory/hosts playbooks/check_local_users.yml --extra-vars "target_servers=kmaster1,lb1,lb2" --private-key=automation-key

                }
            }
        }
    }
}
